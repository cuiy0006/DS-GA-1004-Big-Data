#!/usr/bin/env python

import sys

cnt = 0
currentkey = None

for line in sys.stdin:
	continue
# 	line = line.strip()
# 	key, value = line.split('\t', 1)
	
# 	if key == currentkey:
# 		cnt += 1
# 	else:
# 		if currentkey:
# 			print('{0:s}\t{1:d}'.format(currentkey, cnt))
# 		currentkey = key
# 		cnt = 1

# if currentkey == key:
# 	print('{0:s}\t{1:d}'.format(currentkey, cnt))
